<% if f.object.class.multiple? key %>
    <%= f.input :institution_url, as: :single_value_with_help, required: f.object.required?(key)  %>
<% else %>
    <%= f.input :institution_url, as: :single_value_with_help,  required: f.object.required?(key) %>
<% end %>