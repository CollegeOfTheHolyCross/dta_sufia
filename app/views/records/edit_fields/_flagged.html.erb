<% if f.object.class.multiple? key %>
    <%= f.input :flagged, as: :boolean_with_help, required: f.object.required?(key) %>
<% else %>
    <%= f.input :flagged, as: :boolean_with_help,  required: f.object.required?(key), as: :radio_buttons,  selected: false %>
<% end %>