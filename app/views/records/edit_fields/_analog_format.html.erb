<% if f.object.class.multiple? key %>
    <%= f.input :analog_format, as: :single_value_with_help, required: f.object.required?(key)  %>
<% else %>
    <%= f.input :analog_format, as: :single_value_with_help,  required: f.object.required?(key) %>
<% end %>